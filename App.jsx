import {useEffect,useState} from 'react';import Layout from './components/Layout';import StudentDashboard from './pages/StudentDashboard';import GenericPage from './pages/GenericPage';import {getSession,getProfile} from './lib/api';
export default function App(){const [mode,setMode]=useState('student');const [page,setPage]=useState('home');const [session,setSession]=useState(null);const [name,setName]=useState('Student');useEffect(()=>{getSession().then(({data})=>{setSession(data?.session||null);if(data?.session?.user?.id)getProfile(data.session.user.id).then(({data:p})=>p?.full_name&&setName(p.full_name));});},[]);const adminPages=['students','review','attendance','tests','courses','payments','settings'];const current=mode==='student'&&page==='home'?<StudentDashboard name={name} setPage={setPage}/>:<GenericPage page={page} mode={mode}/>;return <Layout mode={mode} page={page} setPage={setPage} onToggleMode={()=>{setMode(m=>m==='student'?'admin':'student');setPage(mode==='student'?'students':'home')}} studentName={name}>{current}</Layout>}
